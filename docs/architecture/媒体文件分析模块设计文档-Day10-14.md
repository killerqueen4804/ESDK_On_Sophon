# 📘 媒体文件分析模块设计文档 (Day 10-14)

**创建日期**: 2025-11-20  
**架构方案**: 四层架构（遵循 `Task模块实施规划-四层架构.md`）  
**版本**: v1.0  
**状态**: 设计中 🚧

---

## 📋 目录

1. [核心差异分析](#核心差异分析)
2. [架构设计](#架构设计)
3. [关键类设计](#关键类设计)
4. [MQTT 消息处理流程](#mqtt-消息处理流程)
5. [序列图](#序列图)
6. [状态机设计](#状态机设计)
7. [分阶段开发计划](#分阶段开发计划)
8. [测试方案](#测试方案)
9. [风险与注意事项](#风险与注意事项)

---

## 核心差异分析

### 1.1 LiveStreamTask vs MediaFileTask 对比表

| 维度                       | LiveStreamTask (视频流)                                                                           | MediaFileTask (媒体文件)                           |
| -------------------------- | ------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| **数据源**                 | DJI Liveview (实时 H.264 流)                                                                      | DJI MediaManager (历史 JPEG 文件)                  |
| **触发方式**               | 连续解码帧                                                                                        | 文件回调 + 队列处理                                |
| **检测频率**               | 异步检测（主线程 30FPS，检测线程按能力）                                                          | 每张图片必检测                                     |
| **发布策略**               | 实时推流 + 异步 MQTT 推送（检测到目标时）                                                         | 每张图片一次 MQTT 事件（按算法类型分组）           |
| **`device_task_end` 处理** | **立即结束任务** + 推送 `device_task_analysis_result`                                             | **仅标记 `taskEnded_`，不停止任务**                |
| **超时控制**               | 无（流式处理）                                                                                    | 60 秒无新文件 + `taskEnded_=true` → 自动完成       |
| **资源管理**               | 停止流、关闭 RTMP                                                                                 | 删除本地文件 + 恢复 `SetDroneNestAutoDelete(true)` |
| **图片处理**               | 原图检测 + Resize(800x600) 推流                                                                   | 原图检测 + Resize(800x600) MQTT 推送               |
| **GPS 计算**               | 无（实时流无 EXIF）                                                                               | HTTP 批量请求（基于 EXIF 元数据）                  |
| **事件缓存**               | 无                                                                                                | 本地缓存 + MQTT 重连重试                           |
| **共同点**                 | ✅ 都调用 `TaskService::processFrame()`<br>✅ 都通过 `TaskManager` 管理<br>✅ 都继承 `ITask` 接口 |

---

### 1.2 关键差异点详解

#### ⭐ 差异 1: `device_task_end` 处理逻辑

```cpp
// LiveStreamTask 的处理
void LiveStreamTask::onTaskEnd() {
    logger_.info("📢 收到 device_task_end，视频流任务立即结束");

    // 1. 立即停止任务
    stop();

    // 2. 推送"任务分析完成"
    publishTaskAnalysisResult(true);  // result=0 成功

    // 3. 等待平台发送 device_algorithm_disable
}

// MediaFileTask 的处理
void MediaFileTask::onTaskEnd() {
    logger_.info("📢 收到 device_task_end，标记航线结束，继续等待文件");

    // 1. 仅标记标志，不停止任务 ⭐
    taskEnded_.store(true);

    // 2. 任务继续运行，等待文件传输
    // 3. 当满足条件时自动完成：
    //    - taskEnded_ == true
    //    - 60秒无新文件
}
```

#### ⭐ 差异 2: 任务完成条件

```cpp
// LiveStreamTask: 外部控制结束
bool LiveStreamTask::shouldStop() {
    // 1. 收到 stop() 调用
    // 2. 收到 device_task_end
    // 3. 发生错误
    return !running_.load();
}

// MediaFileTask: 自动判断结束
bool MediaFileTask::shouldComplete() {
    std::lock_guard<std::mutex> lock(timerMutex_);

    // 条件 1: 航线任务已结束
    if (!taskEnded_.load()) {
        return false;
    }

    // 条件 2: 60 秒无新文件
    auto now = std::chrono::steady_clock::now();
    auto elapsed = std::chrono::duration_cast<std::chrono::seconds>(
        now - lastFileTime_
    ).count();

    return elapsed >= TIMEOUT_SECONDS;
}
```

---

## ITask 接口扩展设计

### 2.1 添加 onTaskEnd() 虚函数

**修改文件**: `include/esdk_sophon/task/ITask.h`

```cpp
class ITask {
public:
    // ... 现有方法

    /**
     * @brief 处理"航线任务结束"通知（device_task_end）⭐
     *
     * 不同任务类型的处理逻辑不同：
     * - LiveStreamTask: 立即停止任务，推送完成消息
     * - MediaFileTask: 仅标记结束，继续等待文件传输
     *
     * 状态转换（取决于具体实现）:
     * - LiveStreamTask: RUNNING → COMPLETED
     * - MediaFileTask: RUNNING → RUNNING (仅设置内部标志)
     *
     * 调用时机: MqttHandler 收到 device_task_end 消息时
     *
     * 实现要求:
     * - 线程安全（可能在 MQTT 线程中调用）
     * - 快速返回（不阻塞 MQTT 线程）
     * - 幂等性（重复调用应该是安全的）
     *
     * @note 默认实现为空（不支持此消息的任务类型）
     * @note 派生类应该根据自己的业务逻辑覆盖此方法
     *
     * 使用示例:
     * @code
     * // 在 MqttHandler::handleTaskEnd() 中
     * auto task = taskMgr.getTask(taskId);
     * if (task) {
     *     task->onTaskEnd();  // 多态调用，自动路由到正确的实现
     * }
     * @endcode
     */
    virtual void onTaskEnd() {
        // 默认实现：忽略此消息
        // 不支持 task_end 的任务类型不需要覆盖此方法
    }

protected:
    // ... 现有方法
};
```

### 2.2 LiveStreamTask 实现

**修改文件**: `src/task/LiveStreamTask.h`

```cpp
class LiveStreamTask : public ITask {
public:
    // ... 现有方法

    /**
     * @brief 处理"航线任务结束"（视频流任务特有逻辑）⭐
     *
     * 流程:
     * 1. 立即停止任务（调用 stop()）
     * 2. 推送"任务分析完成"消息
     * 3. 等待平台发送 device_algorithm_disable
     */
    void onTaskEnd() override;

private:
    // ... 现有成员
};
```

**修改文件**: `src/task/LiveStreamTask.cpp`

```cpp
void LiveStreamTask::onTaskEnd() {
    logger_.info("📢 [LiveStreamTask] 收到 device_task_end，立即结束任务");

    // 1. 停止任务
    stop();

    // 2. 推送"任务分析完成"
    publishTaskAnalysisResult(true);

    logger_.info("✅ [LiveStreamTask] 任务已结束，等待 device_algorithm_disable");
}

void LiveStreamTask::publishTaskAnalysisResult(bool success) {
    nlohmann::json event;
    event["method"] = "device_task_analysis_result";
    event["taskID"] = config_.taskId;
    event["result"] = success;

    std::string topic = "thing/product/" + config_.deviceSn + "/events";
    mqttClient_->publish(topic, event.dump());

    logger_.info("✅ 已推送 device_task_analysis_result");
}
```

### 2.3 MediaFileTask 实现

**修改文件**: `src/task/MediaFileTask.h`

```cpp
class MediaFileTask : public ITask {
public:
    // ... 现有方法

    /**
     * @brief 处理"航线任务结束"（媒体文件任务特有逻辑）⭐
     *
     * 流程:
     * 1. 设置 taskEnded_ 标志为 true
     * 2. 任务继续运行，等待文件传输
     * 3. 当满足完成条件时（60秒无新文件），自动结束并推送完成消息
     */
    void onTaskEnd() override;

private:
    std::atomic<bool> taskEnded_{false};  // ⭐ 航线是否结束标志
    // ... 现有成员
};
```

**修改文件**: `src/task/MediaFileTask.cpp`

```cpp
void MediaFileTask::onTaskEnd() {
    logger_.info("📢 [MediaFileTask] 收到 device_task_end，标记任务结束但继续等待文件");

    // 仅设置标志，不停止任务
    taskEnded_.store(true);

    logger_.info("✅ [MediaFileTask] 已标记 taskEnded_=true，将在60秒无新文件后自动完成");
}
```

### 2.4 MqttHandler 简化实现

**修改文件**: `src/Mqtt/MqttHandler.cpp`

```cpp
void MqttHandler::handleTaskEnd(const nlohmann::json& msg) {
    std::string taskId = msg["taskID"];
    logger_.info("📥 收到 device_task_end: " + taskId);

    // 1. 立即返回成功 ⭐
    publishReply("device_task_end", taskId, 0);
    logger_.info("✅ 已回复 device_task_end 成功");

    // 2. 获取任务
    auto& taskMgr = TaskManager::getInstance();
    auto task = taskMgr.getTask(taskId);

    if (!task) {
        logger_.warning("⚠️ 任务不存在: " + taskId);
        return;
    }

    // 3. 多态调用 onTaskEnd() ⭐⭐⭐
    // 不需要判断任务类型，由各任务类自己实现逻辑
    task->onTaskEnd();
}
```

---

## 其他类似方法检查

### 3.1 `device_algorithm_disable` 处理

✅ **不需要区分**，两个任务的处理逻辑相同：

```cpp
void MqttHandler::handleAlgorithmDisable(const nlohmann::json& msg) {
    std::string taskId = msg["taskID"];

    // 立即返回成功
    publishReply("device_algorithm_disable", taskId, 0);

    // 停止并移除任务（通用逻辑）
    auto& taskMgr = TaskManager::getInstance();
    auto task = taskMgr.getTask(taskId);

    if (task) {
        task->stop();  // ITask 接口，统一调用
        taskMgr.removeTask(taskId);
    }
}
```

### 3.2 `start()` 方法

✅ **不需要新接口**，已在 ITask 中定义，两个任务各自实现：

```cpp
// LiveStreamTask::start()
bool LiveStreamTask::start() {
    // 1. 初始化 Liveview
    // 2. 初始化 RTMP
    // 3. 启动解码线程 + 检测线程
}

// MediaFileTask::start()
bool MediaFileTask::start() {
    // 1. 加载检测模型
    // 2. SetDroneNestAutoDelete(false)
    // 3. RegisterMediaFilesObserver
    // 4. 启动文件处理线程
}
```

### 3.3 `stop()` 方法

✅ **不需要新接口**，已在 ITask 中定义，两个任务各自实现资源清理：

```cpp
// LiveStreamTask::stop()
void LiveStreamTask::stop() {
    // 1. 停止 Liveview
    // 2. 停止 RTMP
    // 3. 停止检测线程
}

// MediaFileTask::stop()
void MediaFileTask::stop() {
    // 1. 删除本地文件
    // 2. SetDroneNestAutoDelete(true) ⭐
    // 3. UnregisterMediaFilesObserver
}
```

---

## 总结：接口设计原则

### ✅ 需要在 ITask 中定义接口的场景

| 场景                         | 是否需要接口 | 理由                                                            |
| ---------------------------- | ------------ | --------------------------------------------------------------- |
| 不同任务**处理逻辑不同**     | ✅ 需要      | 如 `onTaskEnd()`：LiveStreamTask 立即停止，MediaFileTask 仅标记 |
| MqttHandler 需要**统一调用** | ✅ 需要      | 避免类型判断（`dynamic_cast`），利用多态                        |
| 生命周期**通用方法**         | ✅ 需要      | 如 `start()`, `stop()`：所有任务都需要                          |

### ❌ 不需要在 ITask 中定义接口的场景

| 场景                     | 是否需要接口 | 理由                                                |
| ------------------------ | ------------ | --------------------------------------------------- |
| 外部不调用的**内部方法** | ❌ 不需要    | 如 `execute()`：只在任务内部使用                    |
| 任务特有的**私有逻辑**   | ❌ 不需要    | 如 `MediaFileTask::processFile()`：只有媒体任务需要 |
| 已有接口可以复用         | ❌ 不需要    | 如资源清理：复用 `stop()`                           |

---

## 改进后的架构优势

### 1. 符合开闭原则（OCP）

```cpp
// 新增任务类型时，只需实现接口，不修改 MqttHandler
class NewTask : public ITask {
    void onTaskEnd() override {
        // 新任务的特有逻辑
    }
};
```

### 2. 符合里氏替换原则（LSP）

```cpp
// 所有 ITask 子类都可以替换基类使用
TaskPtr task = getTask(taskId);
task->onTaskEnd();  // 多态调用，无需知道具体类型
```

### 3. 消除类型判断

```cpp
// ❌ 改进前（不好的设计）
if (auto liveTask = dynamic_cast<LiveStreamTask*>(task.get())) {
    liveTask->stopImmediately();
} else if (auto mediaTask = dynamic_cast<MediaFileTask*>(task.get())) {
    mediaTask->markEnded();
}

// ✅ 改进后（好的设计）
task->onTaskEnd();  // 多态自动路由
```

---

**创建者**: AI Assistant  
**审核者**: TBD  
**最后更新**: 2025-11-20  
**版本**: v1.0  
**状态**: ✅ 设计完成，待实施
