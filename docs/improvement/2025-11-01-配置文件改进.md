# 配置文件改进 - 避免硬编码路径

**日期**: 2025-11-01  
**改进目标**: 将模型路径从代码迁移到 config.json,支持灵活配置

---

## 🎯 改进动机

### 问题

原先的设计(包括测试代码)中存在硬编码路径:

```cpp
// ❌ 硬编码 - 不好的做法
config.modelPath = "/data/Edge-SDK/models/ppyoloe_crn_m_300e_coco_289.bmodel";
config.configFile = "/data/Edge-SDK/models/infer_cfg.yml";
```

**硬编码的缺点**:

1. 修改路径需要重新编译
2. 不同环境(开发/测试/生产)无法灵活切换
3. 不利于团队协作(每个人的路径可能不同)
4. 无法动态配置和热更新

### 解决方案

将路径配置到`config.json`,代码从配置文件读取:

```cpp
// ✅ 从配置文件读取 - 好的做法
VisionConfigLoader loader;
auto config = loader.loadDetectorConfig("ppyoloe");
// config.modelPath 从 config.json 中读取
```

---

## 📝 修改内容

### 1. 更新 config.json

**文件**: `config/config.json`

#### 1.1 PP-YOLOE 检测器配置

```json
{
  "detector": {
    "ppyoloe": {
      "_comment": "PP-YOLOE目标检测模型 (PaddleDetection)",
      "model_path": "/data/Edge-SDK/models/PaddleDetection/ppyoloe_crn_m_300e_coco_289.bmodel",
      "config_path": "/data/Edge-SDK/models/PaddleDetection/infer_cfg.yml",
      "labels_path": "models/coco.names",
      "confidence_threshold": 0.5,
      "nms_threshold": 0.45,
      "input_size": [640, 640],
      "device_id": 0
    },
    "yolov10": {
      "_comment": "YOLOv10备用配置 (已弃用,保留以防万一)",
      "enabled": false,
      "model_path": "models/yolov10n.bmodel",
      "config_path": "models/yolov10.yaml",
      "labels_path": "models/coco.names"
    }
  }
}
```

**关键配置项**:

- `model_path`: PP-YOLOE 模型文件(.bmodel)
- `config_path`: PaddleDetection 配置文件(infer_cfg.yml)
- `labels_path`: 类别标签文件(coco.names)
- `confidence_threshold`: 置信度阈值(0.5)
- `nms_threshold`: NMS 阈值(0.45)
- `input_size`: 输入尺寸[width, height]

#### 1.2 PaddleSeg 分割器配置

```json
{
  "segmentation": {
    "paddleseg": {
      "_comment": "PaddleSeg语义分割模型",
      "enabled": true,
      "model_path": "/data/Edge-SDK/models/paddleSeg/pp_liteseg.bmodel",
      "config_path": "/data/Edge-SDK/models/paddleSeg/deploy.yaml",
      "num_classes": 21,
      "input_size": [512, 512]
    }
  }
}
```

**关键配置项**:

- `enabled`: 是否启用分割模块
- `model_path`: PaddleSeg 模型文件(.bmodel)
- `config_path`: PaddleSeg 配置文件(deploy.yaml)
- `num_classes`: 分割类别数量
- `input_size`: 输入尺寸[width, height]

---

### 2. 创建 VisionConfigLoader

**目的**: 从 Config 单例读取 vision 模块配置,转换为对应的配置结构体

#### 2.1 头文件

**文件**: `include/esdk_sophon/vision/VisionConfigLoader.h`

**关键类和方法**:

```cpp
/**
 * @brief 视觉配置加载器
 */
class VisionConfigLoader {
public:
    VisionConfigLoader();

    /**
     * @brief 加载检测器配置
     * @param detectorType 检测器类型 (如"ppyoloe", "yolov10")
     * @return DetectorConfig 检测器配置结构体
     */
    DetectorConfig loadDetectorConfig(const std::string& detectorType = "ppyoloe") const;

    /**
     * @brief 加载分割器配置
     * @param segmentorType 分割器类型 (如"paddleseg")
     * @return SegmentorConfig 分割器配置结构体
     */
    SegmentorConfig loadSegmentorConfig(const std::string& segmentorType = "paddleseg") const;

    /**
     * @brief 加载类别标签文件
     * @param labelsPath 标签文件路径
     * @return std::vector<std::string> 类别名称列表
     */
    std::vector<std::string> loadClassLabels(const std::string& labelsPath) const;

private:
    core::Config& config_;     ///< Config单例引用
    core::Logger& logger_;     ///< Logger单例引用

    std::vector<std::string> getDefaultCocoLabels() const;
};
```

**新增数据结构**:

```cpp
/**
 * @brief 分割器配置结构体
 */
struct SegmentorConfig {
    std::string modelPath;     ///< 模型文件路径
    std::string configPath;    ///< 配置文件路径
    int numClasses;            ///< 分割类别数量
    int inputWidth;            ///< 输入宽度
    int inputHeight;           ///< 输入高度
    bool enabled;              ///< 是否启用
};
```

#### 2.2 实现文件

**文件**: `src/vision/VisionConfigLoader.cpp`

**核心逻辑**:

```cpp
DetectorConfig VisionConfigLoader::loadDetectorConfig(const std::string& detectorType) const {
    DetectorConfig config;

    // 构造配置键前缀: "detector.ppyoloe.xxx"
    std::string prefix = "detector." + detectorType + ".";

    // 从config.json读取各项配置
    config.modelPath = config_.getString(prefix + "model_path", "");
    config.configFile = config_.getString(prefix + "config_path", "");
    config.confidenceThreshold = static_cast<float>(
        config_.getDouble(prefix + "confidence_threshold", 0.5)
    );
    // ... 其他配置项

    return config;
}
```

**配置键映射**:

| config.json 路径                        | DetectorConfig 字段   |
| --------------------------------------- | --------------------- |
| `detector.ppyoloe.model_path`           | `modelPath`           |
| `detector.ppyoloe.config_path`          | `configFile`          |
| `detector.ppyoloe.confidence_threshold` | `confidenceThreshold` |
| `detector.ppyoloe.nms_threshold`        | `nmsThreshold`        |
| `detector.ppyoloe.input_size[0]`        | `inputWidth`          |
| `detector.ppyoloe.input_size[1]`        | `inputHeight`         |

---

### 3. 更新 CMakeLists.txt

**文件**: `src/vision/CMakeLists.txt`

添加 vision_common 库:

```cmake
# ----------------------------------------------------------------------------
# 2. 创建vision_common库（包含VisionConfigLoader等通用类）
# ----------------------------------------------------------------------------
add_library(vision_common STATIC
    VisionConfigLoader.cpp
)

target_include_directories(vision_common
    PUBLIC
        ${PROJECT_SOURCE_DIR}/include
)

target_link_libraries(vision_common
    PUBLIC
        core    # Config、Logger等核心模块
)

message(STATUS "配置vision_common库: VisionConfigLoader")
```

更新 vision 顶层接口库:

```cmake
target_link_libraries(vision
    INTERFACE
        vision_common   # 配置加载器等通用功能
        detector        # 目标检测
        segmentation    # 图像分割
        stream          # 视频流处理
)
```

---

### 4. 创建测试用例

**文件**: `tests/test_vision_config.cpp`

**测试内容**:

1. **test1_LoadPPYoloeConfig**: 加载 PP-YOLOE 配置

   - 验证模型路径、配置文件路径
   - 检查置信度阈值、输入尺寸等参数

2. **test2_LoadPaddleSegConfig**: 加载 PaddleSeg 配置

   - 验证模型路径、配置文件路径
   - 检查类别数量、输入尺寸等参数

3. **test3_ConfigConsistency**: 配置一致性验证

   - 两次加载相同配置应得到相同结果

4. **test4_LoadClassLabels**: 类别标签加载
   - 测试默认 COCO 标签(80 类)
   - 验证标签文件解析逻辑

**运行方式**:

```bash
# 使用默认config.json
cd /workspace/ESDK_On_Sophon/build
make test_vision_config
./bin/tests/test_vision_config

# 使用自定义配置文件
./bin/tests/test_vision_config /path/to/config.json
```

**预期输出**:

```
========================================
测试1: 加载PP-YOLOE配置
========================================

=== 检测器配置 ===
模型路径: /data/Edge-SDK/models/PaddleDetection/ppyoloe_crn_m_300e_coco_289.bmodel
配置文件: /data/Edge-SDK/models/PaddleDetection/infer_cfg.yml
类别数量: 80
置信度阈值: 0.5
NMS阈值: 0.45
输入尺寸: 640x640
✅ 模型路径正确
✅ 配置文件路径正确

✅ 测试1通过: PP-YOLOE配置加载成功

...

✅ 所有测试通过!
```

---

### 5. 更新 tests/CMakeLists.txt

```cmake
# VisionConfigLoader测试程序
add_executable(test_vision_config test_vision_config.cpp)

target_link_libraries(test_vision_config
    PRIVATE
        vision_common    # VisionConfigLoader模块
        core             # Config、Logger模块
        pthread
)

set_target_properties(test_vision_config PROPERTIES
    RUNTIME_OUTPUT_DIRECTORY "${PROJECT_BINARY_DIR}/bin/tests"
)

message(STATUS "配置测试程序: test_vision_config")
```

---

## 🚀 使用方式

### 在 Application 中使用

```cpp
// main.cpp 或 Application.cpp

#include "esdk_sophon/vision/VisionConfigLoader.h"
#include "esdk_sophon/vision/PPYoloeDetector.h"
#include "esdk_sophon/core/Config.h"

int main() {
    // 1. 加载全局配置文件
    auto& config = core::Config::getInstance();
    config.load("config/config.json");

    // 2. 使用VisionConfigLoader加载检测器配置
    vision::VisionConfigLoader loader;
    auto detectorConfig = loader.loadDetectorConfig("ppyoloe");

    // 3. 初始化检测器
    auto detector = std::make_unique<vision::PPYoloeDetector>();
    if (!detector->initialize(detectorConfig)) {
        std::cerr << "检测器初始化失败" << std::endl;
        return 1;
    }

    // 4. 使用检测器
    cv::Mat image = cv::imread("test.jpg");
    auto results = detector->detect(image);

    return 0;
}
```

### 在其他模块中使用

```cpp
// 任务启动时加载配置
void startDetectionTask() {
    vision::VisionConfigLoader loader;
    auto config = loader.loadDetectorConfig("ppyoloe");

    // 传递给检测器
    detector_->initialize(config);
}
```

---

## 📚 知识点总结

### 1. 配置管理最佳实践

#### 为什么要避免硬编码?

**硬编码的问题**:

```cpp
// ❌ 硬编码
std::string modelPath = "/data/Edge-SDK/models/ppyoloe.bmodel";
```

缺点:

1. 修改路径需要重新编译
2. 不同环境难以切换
3. 不利于团队协作
4. 无法动态配置

**配置文件的优势**:

```cpp
// ✅ 从配置文件读取
std::string modelPath = config_.getString("detector.ppyoloe.model_path");
```

优点:

1. 无需重新编译即可修改
2. 支持多环境配置
3. 便于版本控制和部署
4. 支持热更新(reload)

#### 面试要点

**Q: 软件中的配置应该如何管理?**

A: 分层管理配置:

1. **编译时配置**: 宏定义、模板参数 (不可变)
2. **启动时配置**: 配置文件、环境变量 (可变但需重启)
3. **运行时配置**: 远程配置中心、数据库 (热更新)

本项目采用启动时配置(config.json),平衡了灵活性和复杂度。

**Q: 如何处理配置缺失?**

A: 三种策略:

1. **严格模式**: 缺少必需配置时抛出异常/退出程序
2. **默认值**: 提供合理默认值,记录警告日志
3. **交互提示**: 询问用户提供缺失配置

本项目采用默认值策略,适合自动化部署场景。

---

### 2. 单例模式的应用

Config 和 Logger 都是单例,VisionConfigLoader 通过引用访问:

```cpp
VisionConfigLoader::VisionConfigLoader()
    : config_(core::Config::getInstance())      // 获取Config单例
    , logger_(core::Logger::getInstance()) {    // 获取Logger单例
    // ...
}
```

**为什么用引用而不是指针?**

1. 单例保证一直存在,不会为 nullptr
2. 引用语义更清晰(不可重新绑定)
3. 避免 nullptr 检查的开销

---

### 3. 字符串拼接技巧

```cpp
std::string prefix = "detector." + detectorType + ".";
config.modelPath = config_.getString(prefix + "model_path", "");
```

这样可以灵活支持多种检测器类型:

- `detectorType = "ppyoloe"` → `"detector.ppyoloe.model_path"`
- `detectorType = "yolov10"` → `"detector.yolov10.model_path"`

---

### 4. JSON 数组配置的处理

JSON 中的数组 `"input_size": [640, 640]` 如何读取?

**方案 1**: 如果 Config 支持数组

```cpp
auto sizes = config_.getArray("detector.ppyoloe.input_size");
config.inputWidth = sizes[0];
config.inputHeight = sizes[1];
```

**方案 2**: 使用索引语法 (本项目采用)

```cpp
config.inputWidth = config_.getInt("detector.ppyoloe.input_size[0]", 640);
config.inputHeight = config_.getInt("detector.ppyoloe.input_size[1]", 640);
```

**方案 3**: 分别配置 width 和 height

```json
{
  "detector": {
    "ppyoloe": {
      "input_width": 640,
      "input_height": 640
    }
  }
}
```

本项目同时支持方案 2 和方案 3 作为备用。

---

## ✅ 验证步骤

### 1. 编译

```bash
cd /workspace/ESDK_On_Sophon/build
cmake ..
make vision_common
make test_vision_config
```

### 2. 运行测试

```bash
./bin/tests/test_vision_config
```

### 3. 预期结果

所有 4 个测试通过:

- ✅ test1: PP-YOLOE 配置加载
- ✅ test2: PaddleSeg 配置加载
- ✅ test3: 配置一致性验证
- ✅ test4: 默认标签加载

---

## 📁 修改的文件清单

### 新增文件

1. `include/esdk_sophon/vision/VisionConfigLoader.h` (220 行)
2. `src/vision/VisionConfigLoader.cpp` (330 行)
3. `tests/test_vision_config.cpp` (360 行)

### 修改文件

1. `config/config.json`

   - 更新 detector 配置(ppyoloe 替换 yolov10)
   - 新增 segmentation 配置(paddleseg)

2. `src/vision/CMakeLists.txt`

   - 添加 vision_common 库
   - 更新 vision 接口库依赖

3. `tests/CMakeLists.txt`
   - 添加 test_vision_config 测试程序

---

## 🎓 经验总结

1. **配置集中管理**: 所有路径都在 config.json 中,避免散落在代码各处
2. **接口统一**: VisionConfigLoader 提供统一的配置加载接口
3. **容错处理**: 配置缺失时使用默认值并记录警告
4. **便于测试**: 测试时可以使用不同的 config.json
5. **面向未来**: 新增检测器/分割器只需修改配置文件

---

**创建人**: GitHub Copilot  
**创建日期**: 2025-11-01  
**文档版本**: v1.0
