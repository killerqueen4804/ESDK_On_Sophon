### 配置

将 frp 文件放到我(lzy)的文件夹下就可以
打开 frpc.toml

```

serverAddr = "117.132.4.173"
serverPort = 7000
loginFailExit=false

[transport]
dialServerTimeout=60


[[proxies]]
name = "ssh4090-4"
type = "tcp"
localIP = "127.0.0.1"
localPort = 22
remotePort = 6005

```

一般只需要修改 name 和 remotePort，这次的我都修改好了.
运行方式: ./frpc -c ./frpc.toml

### 编写脚本

运行成功后新建一个 frpc.sh

```
cd /data/frp

./frpc -c ./frpc.toml
```

"/data/frp"换成 frp 文件夹所在的路径

### 设置自启

sudo nano /etc/systemd/system/frpc.service
将下面的内容复制进去

```
[Unit]
Description=My terraform_start APP
After=syslog.target

[Service]
User=admin
ExecStart=/bin/bash /data/Edge-SDK/frpc.sh #运行frpc.sh脚本
SuccessExitStatus=143
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
```

注意更换 frpc.sh 的路径

```
重新加载配置
sudo systemctl daemon-reload

设置开机自启
sudo systemctl enable frpc.service

启动服务
sudo systemctl start frpc.service

查看服务状态
sudo systemctl status frpc.service
```

sudo systemctl status frpc.service 如果没报错，那就说明可以了
